# タスクリスト

## 作業状態

- **状態**: active
- **状態更新日時**: {YYYY-MM-DDTHH:MM:SS+TZ}
- **使用ハーネス**: {Claude Code / Codex / Kiro}

## 作業履歴

_記録なし_

## タスク管理の原則

- `active`: 作業系列が継続中。未完了を許容する
- `paused`: 意図的な中断。有効な中断記録がある場合に未完了を許容する
- `complete`: 全タスクと振り返りが完了。未完了を許容しない
- 完了・技術的スキップは実態に合わせて即時記録する
- 「今日はここまで」はスキップではなく`scripts/steering_state.py pause`で記録する
- 最終品質ゲート、コミット、G3受け入れ記録、push、PRはチェックボックスにしない

### フェーズとadd-featureステップ

チェックボックスは、消化するステップごとに排他的に分割する。ステップ5の実装ループが消化するのは実装フェーズだけである。

| フェーズ | 消化するステップ |
|---|---|
| 実装フェーズ（フェーズ1〜N） | ステップ5 |
| 4段検証 | ステップ6 |
| 振り返りとドキュメント更新 | ステップ7 |
| `complete`遷移・最終品質ゲート・PR | ステップ7終端・ステップ8（手順管理） |

技術的理由でタスクが不要になった場合だけ、取り消し線と具体的理由を付けて完了扱いにする。時間や難易度を理由に使わない。

---

## フェーズ1: {フェーズ名}（実装フェーズ / ステップ5）

- [ ] {タスク1}
  - [ ] {サブタスク1-1}
  - [ ] {サブタスク1-2}
- [ ] {タスク2}

## フェーズ2: {フェーズ名}（実装フェーズ / ステップ5）

- [ ] {タスク1}
- [ ] {タスク2}

> 実装フェーズは必要な数だけ置く。実装フェーズがすべて完了したらステップ6へ進み、後続フェーズをステップ5で先取りしない。

## フェーズ3: 4段検証（ステップ6）

- [ ] 段1: 静的検証
  - [ ] `{テストコマンド}`
  - [ ] `{リントコマンド}`
  - [ ] `{型チェックコマンド}`
- [ ] 段2: 実挙動検証
- [ ] 段3: コードレビューと指摘対応
- [ ] 段4: スペック準拠検証と指摘対応

## フェーズ4: 振り返りとドキュメント更新（ステップ7）

- [ ] 永続ドキュメントの更新要否を判断し、必要な更新とレビューを完了
- [ ] README類の更新要否を判断し、必要なら更新
- [ ] 実装後の振り返りを記録
- [ ] 全テスト通過、lintエラーなし、リリース判断を記録

> 上の全チェック完了後、`python3 scripts/steering_state.py complete --harness {ハーネス}`で`complete`へ遷移する。その後、add-featureステップ8で最終品質ゲートを1回実行する。ゲート失敗時は`resume`で`active`へ戻し、修正・再検証・振り返り更新・`complete`遷移をやり直す。

---

## 実装後の振り返り

### 実装完了日

{YYYY-MM-DD}

### 計画と実績の差分

**計画と異なった点**:

- {差分と理由}

**新たに必要になったタスク**:

- {追加タスクと理由}

**技術的理由でスキップしたタスク**:

- {該当なし、またはタスク名・理由・代替}

### 学んだこと

**技術的な学び**:

- {技術的知見}

**プロセス上の改善点**:

- {プロセス上の知見}

### 次回への改善提案

- {改善提案}

### リリース判断

| 観点 | 評価 |
|---|---|
| ユーザー価値のあるまとまりか | {Yes / No / 保留} |
| 未解決の重大バグ | {なし / あり: 内容} |
| 適切なバージョン種別 | {MAJOR / MINOR / PATCH / リリース不要} |

**提案**:

{リリース提案と理由}
