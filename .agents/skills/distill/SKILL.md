---
name: distill
description: ステアリングの振り返りを横断収集し、環流3基準で分類して環流候補のPR下書きを提示する蒸留スキル。プロジェクトの節目や「distillを実行して」と依頼されたときに使う。
---

# Distill スキル(蒸留・Codexアダプタ)

**手順の正は `docs/procedures/distill.md`(ハーネス中立の手順書)にある。必ず読み込み、処理手順1〜6に従うこと。**

## Codex 固有の注記

- 手順書の「ファイル列挙」はシェル(`ls` / `find`)またはファイル検索機能で行う
- 手順書の「記憶層」について、Codexにプロジェクト用の永続記憶機構を設定していない場合は、手順書の規定どおり `.steering/` の振り返りへの記録で代替する
