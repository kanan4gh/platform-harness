---
name: review-docs
description: プロジェクトドキュメントの品質を詳細レビューする。「review-docsを実行して」「〜をレビューして」とドキュメントのレビューを依頼されたときに使う。
---

# ドキュメントレビュー(Codexアダプタ)

**手順の正は `docs/procedures/review-docs.md`(ハーネス中立の手順書)にある。必ず読み込み、5観点・種別別チェック・レポート形式に従うこと。**

## Codex 固有の注記

- 手順書の「独立した文脈での実行」は `.codex/agents/doc-reviewer.toml` のサブエージェントへの委譲で実現する(利用できない場合は、レビュー専用の別セッションで実施する)
